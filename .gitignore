__pycache__/
*.pyc
.DS_Store
dist/
build/
*.egg-info/
.venv/

# 빌드 부산물 — 썸네일 생성 도구(_workspace/07_thumbnail)의 의존성.
# 1.2.x sdist 가 playwright-core 를 PyPI 로 실어 보낸 원인이었다.
node_modules/

# Claude Code 로컬 설정(로컬 절대경로 포함)
.claude/settings.local.json

# 프로브 캡처(세션 경로·머신 고유값 포함)
tools/_probe/

# 조직 가명화 salt — 커밋 금지
*.tokenhabit-org-salt
tools/_probe/salt
